@model IEnumerable<PlantInventory.Models.MoveModels.MoveDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Move Plants", "Create", new { id = Model.ToArray().FirstOrDefault().BatchId })
    @Html.ActionLink("Back To Batches", "GetBatchByID", "Batch", new { id = Model.ToArray().FirstOrDefault().BatchId }, null)
</p>
<table class="table">
    <tr>
        
        <th>
            @Html.DisplayNameFor(model => model.NumberOfPotsMoved)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateMoved)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MoveFrom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MoveTo)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>

    <td>
        @Html.DisplayFor(modelItem => item.NumberOfPotsMoved)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Comment)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.DateMoved)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.MoveFrom)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.MoveTo)
    </td>

    <td>
        @Html.ActionLink("Edit", "EditMove", new { id=item.MoveId }) |
   
    </td>
</tr>
}

</table>
